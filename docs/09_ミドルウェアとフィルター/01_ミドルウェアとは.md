# ミドルウェアとは

ミドルウェアは、HTTP リクエストとレスポンスの通り道に置かれる処理です。

```csharp
app.UseHttpsRedirection();
app.UseAuthentication();
app.UseAuthorization();
```

ログ、例外処理、認証、CORS、静的ファイルなど、多くの機能がミドルウェアとして動きます。

ミドルウェアは **登録した順番** が重要です。

