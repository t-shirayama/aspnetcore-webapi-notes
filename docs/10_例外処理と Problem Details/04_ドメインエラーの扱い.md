# ドメインエラーの扱い

業務ルールに違反した場合は、どの HTTP ステータスに対応させるかを決めます。

例えば、重複登録なら `409 Conflict`、存在しない ID なら `404 Not Found`、入力不足なら `400 Bad Request` が候補になります。

```csharp
return Results.Conflict(new
{
    message = "Email already exists."
});
```

例外を投げるか、結果型を返すかは設計次第ですが、API の外側に出るエラー形式はそろえます。

