# ルートパラメーター

ルートパラメーターは URL の一部を値として受け取る仕組みです。

```csharp
app.MapGet("/articles/{id}", (int id) =>
{
    return Results.Ok(new { id });
});
```

`/articles/10` にアクセスすると、`id` には `10` が入ります。

ID やスラッグのように、**対象リソースを識別する値** はルートパラメーターに向いています。

