# Cookie 認証

Cookie 認証は、ブラウザーに保存された Cookie を使ってユーザーを識別する方式です。

サーバー側でログイン状態を扱いやすく、ブラウザー向けアプリでは自然な選択肢になります。

一方で、API を外部クライアントやスマートフォンアプリから呼ぶ場合は、JWT Bearer 認証の方が扱いやすい場面もあります。

Cookie 認証を使う場合は、`SameSite`、`Secure`、CSRF 対策を考えます。

認証方式は、**誰がどのクライアントから API を呼ぶか** で選びます。

