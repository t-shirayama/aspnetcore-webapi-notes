# サーバーエラーの扱い

サーバーエラーは、クライアントがリクエストを直しても解決できない問題です。

代表的なものは `500 Internal Server Error` です。

```json
{
  "title": "An unexpected error occurred.",
  "status": 500
}
```

`500` を返すときに、例外メッセージやスタックトレースをそのまま返してはいけません。内部構造や秘密情報が外部に漏れる可能性があります。

外部には汎用的なエラーを返し、詳細はログに残します。

