# マイグレーション

マイグレーションは、C# のモデル変更をデータベーススキーマへ反映する仕組みです。

```bash
dotnet ef migrations add InitialCreate
dotnet ef database update
```

チーム開発では、マイグレーションファイルもソース管理に含めます。

本番環境では、アプリ起動時に自動適用するか、デプロイ手順として適用するかを慎重に決めます。

