# 構造化ログ

構造化ログは、ログを文字列だけでなくキーと値の情報として残す考え方です。

```csharp
logger.LogInformation("Created article {ArticleId}", article.Id);
```

`ArticleId` のような値を検索や集計に使えるため、運用時の調査がしやすくなります。

ログメッセージに文字列補間を使うより、テンプレートとして渡す方が構造化ログに向いています。

