# ライフタイムを間違えると何が起きるか

DI のライフタイムを間違えると、見つけにくい不具合になります。

`Singleton` にリクエストごとの状態を持たせると、ユーザー間で情報が混ざる危険があります。

`Singleton` から `Scoped` のサービスを直接受け取ると、ライフタイムの不整合が起きます。

`DbContext` は通常 `Scoped` です。長時間保持したり、複数スレッドで共有したりする用途には向きません。

基本は、**状態を持つ処理は短いライフタイムに寄せる** と安全です。

