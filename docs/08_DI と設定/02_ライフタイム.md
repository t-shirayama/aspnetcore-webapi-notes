# ライフタイム

DI に登録するサービスにはライフタイムがあります。

| 登録 | 意味 |
| --- | --- |
| `AddSingleton` | アプリ全体で 1 つ |
| `AddScoped` | 1 リクエストで 1 つ |
| `AddTransient` | 取得するたびに新しい |

Web API では、リクエスト単位で扱いたいサービスに `AddScoped` をよく使います。

`DbContext` も通常は Scoped で登録されます。

