# テスト用 DB

データアクセスを含むテストでは、テスト用の DB を用意します。

選択肢には、SQLite のインメモリ、コンテナー上の DB、EF Core InMemory Provider などがあります。

ただし InMemory Provider はリレーショナル DB と挙動が違うため、SQL や制約の確認には向きません。

本番に近い挙動を見たい場合は、実際の DB エンジンを使った統合テストが有効です。

