# クエリ文字列

クエリ文字列は、検索条件やページング条件などを URL の末尾に付ける仕組みです。

```text
GET /articles?keyword=api&page=2
```

Minimal API では引数名で受け取れます。

```csharp
app.MapGet("/articles", (string? keyword, int page = 1) => Results.Ok());
```

一覧の絞り込みや並び替えは、ルートよりもクエリ文字列にすると自然です。

