# 必要な開発環境

ASP.NET Core Web API の開発には .NET SDK が必要です。エディターは Visual Studio、Visual Studio Code、JetBrains Rider などを使えます。

インストール確認は `dotnet --info` または `dotnet --version` で行います。

```bash
dotnet --version
```

SDK が入っていれば、プロジェクト作成、ビルド、実行、テストを `dotnet` コマンドから操作できます。

