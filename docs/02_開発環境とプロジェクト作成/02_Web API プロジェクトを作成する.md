# Web API プロジェクトを作成する

Web API プロジェクトは `dotnet new webapi` で作成できます。

```bash
dotnet new webapi -n SampleApi
cd SampleApi
dotnet run
```

テンプレートには、HTTP API を動かすための基本的な `Program.cs`、設定ファイル、起動設定が含まれます。

作成直後は **まずビルドして実行できること** を確認します。

