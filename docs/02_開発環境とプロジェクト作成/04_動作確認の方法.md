# 動作確認の方法

Web API はブラウザーだけでなく、`curl`、Postman、REST Client、Swagger UI などで確認します。

```bash
curl https://localhost:7001/weatherforecast
```

`GET` はブラウザーでも試せますが、`POST` や `DELETE` ではリクエストボディやヘッダーが必要になります。

開発中は **HTTP メソッド、URL、ステータスコード、レスポンス本文** をセットで確認します。

