# バリデーションエラーの返し方

バリデーションエラーは、クライアントが修正できる入力ミスです。そのため、`400 Bad Request` とエラー内容を返します。

```json
{
  "title": "One or more validation errors occurred.",
  "status": 400,
  "errors": {
    "title": ["The Title field is required."]
  }
}
```

エラー形式をそろえると、フロントエンドや API 利用者が扱いやすくなります。

