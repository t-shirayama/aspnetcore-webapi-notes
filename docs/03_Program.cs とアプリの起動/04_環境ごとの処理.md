# 環境ごとの処理

ASP.NET Core は `Development`、`Staging`、`Production` などの環境名を持ちます。

```csharp
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}
```

開発環境だけ Swagger UI を有効にする、詳細な例外ページを出す、といった使い分けができます。

本番環境では、開発向けの詳細情報を外部に出さないようにします。

