# Web API のセキュリティ

Web API のセキュリティでは、入力、出力、ログ、設定、DB 操作まで考えます。

API は外部から直接呼ばれる入口です。画面で隠していても、HTTP リクエストを作れば呼び出せる場合があります。

そのため、**守るべきルールは API 側で必ず検証する** 必要があります。

認証・認可の詳細は認証と認可の章で扱い、この章では CORS、CSRF、XSS、SQL Injection、Secret 管理を中心に整理します。

セキュリティは最後に追加するものではなく、API 設計の最初から含めるものです。
