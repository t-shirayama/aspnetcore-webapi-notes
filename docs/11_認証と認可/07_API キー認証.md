# API キー認証

API キー認証は、発行されたキーをリクエストに付けて呼び出し元を識別する方式です。

```http
X-Api-Key: xxxxx
```

サーバー間通信や簡易的な外部連携で使われることがあります。

ただし、API キーだけでは「どのユーザーが操作したか」を表しにくく、細かい認可には向きません。

API キーは漏れると悪用されるため、Secret として扱い、Git に置かないようにします。

