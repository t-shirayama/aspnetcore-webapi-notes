# フィルター

Controller では、アクションの前後に処理を差し込むフィルターを使えます。

フィルターには、認可フィルター、アクションフィルター、例外フィルターなどがあります。

```csharp
[ServiceFilter(typeof(LoggingActionFilter))]
public class ArticlesController : ControllerBase
{
}
```

ミドルウェアはアプリ全体の HTTP パイプライン、フィルターは MVC や Controller の処理に近い場所で使います。

