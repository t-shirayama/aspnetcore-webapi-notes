# XSS と API

XSS は、悪意あるスクリプトをページ上で実行させる攻撃です。

API は HTML を直接返さないことが多いですが、API に保存された文字列がフロントエンドで表示されるときに XSS の原因になることがあります。

API 側では、入力を必要以上に信用せず、危険な値を保存・返却する可能性を理解します。

表示時のエスケープは主にフロントエンドの責務ですが、API も **外部入力は信用しない** という前提で設計します。

